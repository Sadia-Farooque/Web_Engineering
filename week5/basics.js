const course = "WEb Eng";
// course="Other"; this gave me error of assignment to constant varaiable as const variable cannot be reassigned

let count=0;
count+=2;
console.log(count);

console.log(0== false);// i predicted tis true as 0 is considered as false in boolean 
console.log (0===false);// i predicted tis false as 0 is number and false is boolean
console.log ([]==false);// i predicted tis true as empty array is considered as false in boolean